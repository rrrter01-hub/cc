-- =========================================================================
-- BLOX FRUITS ULTIMATE AUTONOMOUS BOUNTY ENGINE v8.2 (PVP COMBO PIPELINE)
-- Features:
--  1. Structured One-Shot Combo Chain (Melee ZXC -> Sword ZX -> Fruit ZXCV -> Gun ZX)
--  2. Strict AimLock & Weapon Synchronizer (การันตีคอมโบติด 100% ได้ค่าหัวแน่นอน)
--  3. M1 Weaving Engine (แทรกหมัด M1 ระหว่างสกิลเพื่อเร่งดาเมจและกันหลุดคอมโบ)
--  4. Instant Underground Safe Bunker (-65 Studs Dive on Kill / Low HP)
--  5. Overhead Helix Orbit (เกาะเหนือหัวเป้าหมาย 9.5 Studs หลบสกิล 360°)
--  6. Quick Safety Hub: [🕳️ DIVE], [🕊️ STARTER], [☕ CAFE], [⚔️ COMBO: ON/OFF]
--  7. Real-Time 3D Billboard ESP & Wallhack Radar
-- =========================================================================

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local VirtualUser = game:GetService("VirtualUser")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local IsMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled

-- =========================================================================
-- COORDINATES BY SEA
-- =========================================================================
local StarterLocations = {
    [2753915549] = Vector3.new(1071, 60, 1426),    -- Sea 1: Pirate Starter / Windmill
    [4442272183] = Vector3.new(-380, 80, 298),      -- Sea 2: Starter / Kingdom of Rose
    [7449423635] = Vector3.new(-5085, 320, -3156),  -- Sea 3: Castle on the Sea Hub
}

local CafeLocations = {
    [2753915549] = Vector3.new(1071, 60, 1426),    -- Sea 1: Windmill Safe Zone
    [4442272183] = Vector3.new(-380, 77, 255),     -- Sea 2: Cafe Safe Interior
    [7449423635] = Vector3.new(-12463, 375, -7550), -- Sea 3: Mansion Safe Zone
}

local function GetStarterIslandPos()
    return StarterLocations[game.PlaceId] or Vector3.new(1071, 60, 1426)
end

local function GetCafePos()
    return CafeLocations[game.PlaceId] or Vector3.new(-380, 77, 255)
end

-- =========================================================================
-- CONFIGURATION
-- =========================================================================
local Config = {
    FlySpeed = 320,               -- ความเร็วบินโจมตี
    TravelFlySpeed = 360,         -- ความเร็วบินเดินทางข้ามเกาะ
    TravelSkyAltitude = 850,      -- ความสูงเดินทาง
    
    -- ONE-SHOT SKILL COMBO SETTINGS (ระบบคอมโบสกิลเพื่อค่าหัว)
    EnableSkillCombo = true,      -- เปิดใช้งานระบบคอมโบสกิลต่อเนื่อง
    M1Weaving = true,             -- แทรก M1 ระหว่างปล่อยสกิล
    
    -- UNDERGROUND BUNKER (มุดดินหลบภัย)
    UndergroundDepth = 65,        -- ระยะมุดลงใต้ดิน (Studs)
    AutoDiveOnKill = true,        -- มุดดินหลบทันทีหลังฆ่าสำเร็จ
    
    -- OVERHEAD EVASION (เกาะเหนือหัว + หลบสกิล)
    OverheadHeight = 9.5,         -- ความสูงเหนือหัวศัตรู
    OrbitRadius = 3.0,            -- รัศมีการหมุนวนรอบหัว (Helix Orbit)
    OrbitSpeed = 7.5,             -- ความเร็วการหมุนหลบสกิล
    EnableEvasiveJitter = true,   -- สุ่มจังหวะกระตุกหลบสกิลแนวตั้ง
    
    -- COMBAT & M1
    M1AttackSpeed = 0.05,         -- ความเร็วสแปม M1
    PrimaryWeapon = "Melee",      -- อาวุธหลัก
    HitboxSize = 100,             -- ขยายระยะฮิตบ็อกซ์ลงล่าง
    
    -- ESP SETTINGS
    EnableESP = true,             -- 3D ESP Status
    ESPTextSize = 11,
    
    -- SURVIVAL & RECOVERY
    AutoBuso = true,
    AutoKen = true,
    AutoRaceV3 = true,
    EmergencyEscape = true,
    EmergencyHPThreshold = 42,
    RecoveryHPThreshold = 95
}

local Stats = {
    StartBounty = 0,
    CurrentBounty = 0,
    BountyGained = 0,
    BountyLost = 0,
    Kills = 0,
    Deaths = 0
}

local AppState = {
    SelectedPlayer = nil,
    IsHunting = false,
    IsSpectating = false,
    IsUnderground = false,
    IsRetreating = false,
    IsExecutingCombo = false,
    CurrentCombatStatus = "Idle"
}

-- =========================================================================
-- UTILITY HELPERS
-- =========================================================================
local function GetChar(p) return p and p.Character end
local function GetRoot(c) return c and (c:FindFirstChild("HumanoidRootPart") or c:FindFirstChild("Torso") or c:FindFirstChild("UpperTorso")) end
local function GetHum(c)  return c and c:FindFirstChildOfClass("Humanoid") end

local function GetPlayerBounty(player)
    if not player then return 0 end
    local ls = player:FindFirstChild("leaderstats")
    if ls then
        local b = ls:FindFirstChild("Bounty/Honor") or ls:FindFirstChild("Bounty") or ls:FindFirstChild("Honor")
        if b then return b.Value end
    end
    return 0
end

local function GetPlayerLevel(player)
    if not player then return 1 end
    local data = player:FindFirstChild("Data")
    if data and data:FindFirstChild("Level") then return data.Level.Value end
    local ls = player:FindFirstChild("leaderstats")
    if ls and ls:FindFirstChild("Level") then return ls.Level.Value end
    return 1
end

Stats.StartBounty = GetPlayerBounty(LocalPlayer)
Stats.CurrentBounty = Stats.StartBounty

-- =========================================================================
-- TARGET STATUS CHECKER
-- =========================================================================
local function CheckTargetStatus(player)
    local st = {
        IsAlive = false,
        InSafeZone = false,
        PvPEnabled = true,
        Health = 0,
        MaxHealth = 100,
        Distance = 0,
        Level = 1,
        Bounty = 0,
        CanAttack = false,
        Reason = "OK"
    }

    if not player or not player.Parent then 
        st.Reason = "Invalid Target"
        return st 
    end

    st.Level = GetPlayerLevel(player)
    st.Bounty = GetPlayerBounty(player)

    local char = GetChar(player)
    local myChar = GetChar(LocalPlayer)
    local hum = GetHum(char)
    local root = GetRoot(char)
    local myRoot = GetRoot(myChar)

    if not char or not hum or not root then 
        st.Reason = "Dead / No Character"
        return st 
    end

    st.Health = math.floor(hum.Health)
    st.MaxHealth = math.floor(hum.MaxHealth)
    st.IsAlive = hum.Health > 0

    if myRoot and root then
        st.Distance = math.floor((root.Position - myRoot.Position).Magnitude)
    end

    if char:FindFirstChild("SafeZoneShield") 
        or char:FindFirstChild("InSafeZone") 
        or char:FindFirstChild("ForceField")
        or char:FindFirstChild("SafeZone")
        or player:GetAttribute("InSafeZone") == true
        or player:GetAttribute("SafeZone") == true then
        st.InSafeZone = true
    end

    if char:FindFirstChild("PvPDisabled")
        or char:FindFirstChild("PVPDisabled")
        or player:FindFirstChild("PvPDisabled")
        or player:GetAttribute("PvPDisabled") == true
        or player:GetAttribute("PvP") == false then
        st.PvPEnabled = false
    end

    local teamCheck = true
    if LocalPlayer.Team and player.Team then
        if LocalPlayer.Team.Name == "Marines" and player.Team.Name == "Marines" then
            teamCheck = false
            st.Reason = "Same Marine Team"
        end
    end

    if not st.IsAlive then
        st.Reason = "Player is Dead"
    elseif st.InSafeZone then
        st.Reason = "In SafeZone"
    elseif not st.PvPEnabled then
        st.Reason = "PvP Disabled (Peace)"
    elseif not teamCheck then
        st.Reason = "Same Marine Team"
    else
        st.CanAttack = true
        st.Reason = "Ready for Combat"
    end

    return st
end

-- =========================================================================
-- 3D ESP SYSTEM (NAME, HEALTH, DISTANCE, STATUS)
-- =========================================================================
local ESPFolder = Instance.new("Folder")
ESPFolder.Name = "BountyGod_ESP"
ESPFolder.Parent = (gethui and gethui()) or CoreGui

local ESPObjects = {}

local function CreateESP(player)
    if player == LocalPlayer then return end
    if ESPObjects[player] then return end

    local bill = Instance.new("BillboardGui")
    bill.Name = "ESP_" .. player.Name
    bill.AlwaysOnTop = true
    bill.Size = UDim2.new(0, 180, 0, 50)
    bill.StudsOffset = Vector3.new(0, 3, 0)
    bill.Enabled = Config.EnableESP
    bill.Parent = ESPFolder

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextStrokeTransparency = 0
    label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    label.Font = Enum.Font.GothamBold
    label.TextSize = Config.ESPTextSize
    label.RichText = true
    label.Text = ""
    label.Parent = bill

    local highlight = Instance.new("Highlight")
    highlight.Name = "Highlight_" .. player.Name
    highlight.FillTransparency = 0.65
    highlight.OutlineTransparency = 0.1
    highlight.Enabled = Config.EnableESP
    highlight.Parent = ESPFolder

    ESPObjects[player] = {
        Billboard = bill,
        Label = label,
        Highlight = highlight
    }
end

local function RemoveESP(player)
    if ESPObjects[player] then
        if ESPObjects[player].Billboard then ESPObjects[player].Billboard:Destroy() end
        if ESPObjects[player].Highlight then ESPObjects[player].Highlight:Destroy() end
        ESPObjects[player] = nil
    end
end

for _, p in ipairs(Players:GetPlayers()) do CreateESP(p) end
Players.PlayerAdded:Connect(CreateESP)
Players.PlayerRemoving:Connect(RemoveESP)

RunService.RenderStepped:Connect(function()
    if not Config.EnableESP then
        for _, esp in pairs(ESPObjects) do
            esp.Billboard.Enabled = false
            esp.Highlight.Enabled = false
        end
        return
    end

    for player, esp in pairs(ESPObjects) do
        local char = GetChar(player)
        local root = GetRoot(char)
        local hum = GetHum(char)

        if char and root and hum and hum.Health > 0 and char.Parent then
            esp.Billboard.Adornee = root
            esp.Billboard.Enabled = true
            esp.Highlight.Adornee = char
            esp.Highlight.Enabled = true

            local st = CheckTargetStatus(player)
            local hpPercent = math.floor((hum.Health / math.max(1, hum.MaxHealth)) * 100)

            local statusColor = "#00FF7F"
            local highlightColor = Color3.fromRGB(0, 255, 127)
            local tag = "[READY]"

            if player == AppState.SelectedPlayer then
                statusColor = "#00E5FF"
                highlightColor = Color3.fromRGB(0, 229, 255)
                tag = "[🎯 LOCKED TARGET]"
            elseif st.InSafeZone then
                statusColor = "#FFD700"
                highlightColor = Color3.fromRGB(255, 215, 0)
                tag = "[SAFE ZONE]"
            elseif not st.PvPEnabled then
                statusColor = "#FF5555"
                highlightColor = Color3.fromRGB(255, 85, 85)
                tag = "[PVP OFF]"
            elseif not st.IsAlive then
                statusColor = "#888888"
                highlightColor = Color3.fromRGB(136, 136, 136)
                tag = "[DEAD]"
            end

            esp.Highlight.FillColor = highlightColor
            esp.Highlight.OutlineColor = highlightColor

            esp.Label.Text = string.format(
                "<font color='%s'><b>%s</b></font> <font color='#FFFFFF'>Lv.%d</font><br/>" ..
                "<font color='#00FF7F'>HP: %d%%</font> | <font color='#FFD700'>%dm</font><br/>" ..
                "<font color='%s'><b>%s</b></font>",
                statusColor, player.DisplayName, st.Level,
                hpPercent, st.Distance,
                statusColor, tag
            )
        else
            esp.Billboard.Enabled = false
            esp.Highlight.Enabled = false
        end
    end
end)

-- =========================================================================
-- AUTO BUFFS
-- =========================================================================
local function EnsureBuso()
    if not Config.AutoBuso then return end
    local char = GetChar(LocalPlayer)
    if not char then return end
    if not char:FindFirstChild("HasBuso") then
        local rem = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("CommF_")
        if rem then pcall(function() rem:InvokeServer("Buso") end) end
    end
end

local function EnsureKen()
    if not Config.AutoKen then return end
    local char = GetChar(LocalPlayer)
    if not char then return end
    if not char:FindFirstChild("KenHaki") and not char:FindFirstChild("Observation") then
        local remF = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("CommF_")
        if remF then pcall(function() remF:InvokeServer("Ken", true) end) end
        pcall(function()
            VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.E, false, game)
            task.wait(0.02)
            VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.E, false, game)
        end)
    end
end

local lastRaceTick = 0
local function TriggerRaceSkill()
    if not Config.AutoRaceV3 then return end
    if tick() - lastRaceTick < 15 then return end
    lastRaceTick = tick()

    pcall(function()
        VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.T, false, game)
        task.wait(0.03)
        VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.T, false, game)
    end)
    pcall(function()
        local rem = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("CommF_")
        if rem then rem:InvokeServer("ActivateAbility") end
    end)
end

-- =========================================================================
-- WEAPON CONTROLLER
-- =========================================================================
local function EquipWeapon(weaponType)
    local char = GetChar(LocalPlayer)
    local bp = LocalPlayer:FindFirstChild("Backpack")
    local hum = GetHum(char)
    if not char or not hum then return nil end

    local currentTool = char:FindFirstChildOfClass("Tool")
    if currentTool then
        local tType = currentTool.ToolTip ~= "" and currentTool.ToolTip or currentTool:GetAttribute("WeaponType")
        if not weaponType or tType == weaponType or currentTool.Name:lower():find(weaponType:lower()) then
            return currentTool
        end
    end

    if bp then
        for _, tool in ipairs(bp:GetChildren()) do
            if tool:IsA("Tool") then
                local tType = tool.ToolTip ~= "" and tool.ToolTip or tool:GetAttribute("WeaponType")
                if not weaponType or tType == weaponType or tool.Name:lower():find(weaponType:lower()) then
                    hum:EquipTool(tool)
                    return tool
                end
            end
        end
        local first = bp:FindFirstChildOfClass("Tool")
        if first then
            hum:EquipTool(first)
            return first
        end
    end
    return currentTool
end

-- =========================================================================
-- ADVANCED COMBAT FRAMEWORK HOOK & M1 ENGINE
-- =========================================================================
local CombatFramework = nil
local CombatController = nil

local function ResolveCombatController()
    if CombatController and typeof(CombatController) == "table" then
        return CombatController
    end

    pcall(function()
        local ps = LocalPlayer:FindFirstChild("PlayerScripts")
        if not ps then return end
        local cfModule = ps:FindFirstChild("CombatFramework")
        if cfModule and cfModule:IsA("ModuleScript") then
            CombatFramework = require(cfModule)
            if typeof(CombatFramework) == "table" then
                if CombatFramework.activeController then
                    CombatController = CombatFramework.activeController
                elseif getupvalues then
                    local upvs = getupvalues(CombatFramework)
                    for _, val in pairs(upvs) do
                        if typeof(val) == "table" and rawget(val, "activeController") then
                            CombatController = val.activeController
                            break
                        end
                    end
                end
            end
        end
    end)
    return CombatController
end

local Net = ReplicatedStorage:FindFirstChild("Modules") and ReplicatedStorage.Modules:FindFirstChild("Net")
local RegisterAttack = Net and (Net:FindFirstChild("RE/RegisterAttack") or Net:FindFirstChild("RegisterAttack"))
local RegisterHit = Net and (Net:FindFirstChild("RE/RegisterHit") or Net:FindFirstChild("RegisterHit"))

local comboStep = 1
local function ExecuteM1Combo()
    local myChar = GetChar(LocalPlayer)
    local myHum = GetHum(myChar)
    if not myChar or not myHum or myHum.Health <= 0 then return end

    myHum.Sit = false
    myHum.PlatformStand = false

    local tool = myChar:FindFirstChildOfClass("Tool")

    local targetChar = AppState.SelectedPlayer and GetChar(AppState.SelectedPlayer)
    local targetRoot = targetChar and GetRoot(targetChar)
    local targetHum = targetChar and GetHum(targetChar)

    -- 1. Hook Active Combat Controller
    local controller = ResolveCombatController()
    if controller then
        pcall(function()
            controller.hitboxMagnitude = Config.HitboxSize
            controller.timeToNextAttack = 0
            controller.hitAttempts = 0
            controller.attacking = false
            controller.blocking = false
            
            comboStep = (comboStep % 4) + 1
            controller.increment = comboStep

            if controller.attack then
                controller:attack()
            end
        end)
    end

    -- 2. Direct Network Packet Fire (RegisterAttack & RegisterHit)
    if RegisterAttack then
        pcall(function()
            RegisterAttack:FireServer(0)
        end)
    end

    if RegisterHit and targetRoot and targetHum and targetHum.Health > 0 then
        pcall(function()
            local hitList = {
                { targetChar, targetRoot }
            }
            RegisterHit:FireServer(targetRoot, hitList)
        end)
    end

    -- 3. Tool Direct Trigger & Input Simulation
    if tool then
        pcall(function() tool:Activate() end)
    end

    pcall(function()
        VirtualUser:CaptureController()
        VirtualUser:Button1Down(Vector2.new(0, 0))
        VirtualUser:Button1Up(Vector2.new(0, 0))
    end)
    
    pcall(function()
        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, true, game, 1)
        VirtualInputManager:SendMouseButtonEvent(0, 0, 0, false, game, 1)
    end)
end

-- =========================================================================
-- STRUCTURED ONE-SHOT PVP COMBO PIPELINE (MELEE -> SWORD -> FRUIT -> GUN)
-- =========================================================================
local OneShotComboChain = {
    -- STEP 1: MELEE STUN & DAMAGE
    { Weapon = "Melee", Key = Enum.KeyCode.Z, Hold = 0.12, Delay = 0.28 },
    { Weapon = "Melee", Key = Enum.KeyCode.X, Hold = 0.15, Delay = 0.32 },
    { Weapon = "Melee", Key = Enum.KeyCode.C, Hold = 0.20, Delay = 0.38 },

    -- STEP 2: SWORD BURST STUN
    { Weapon = "Sword", Key = Enum.KeyCode.Z, Hold = 0.15, Delay = 0.30 },
    { Weapon = "Sword", Key = Enum.KeyCode.X, Hold = 0.20, Delay = 0.38 },

    -- STEP 3: BLOX FRUIT HIGH BURST
    { Weapon = "Blox Fruit", Key = Enum.KeyCode.C, Hold = 0.18, Delay = 0.35 },
    { Weapon = "Blox Fruit", Key = Enum.KeyCode.V, Hold = 0.20, Delay = 0.40 },
    { Weapon = "Blox Fruit", Key = Enum.KeyCode.Z, Hold = 0.15, Delay = 0.30 },
    { Weapon = "Blox Fruit", Key = Enum.KeyCode.X, Hold = 0.15, Delay = 0.30 },

    -- STEP 4: GUN FINISHER
    { Weapon = "Gun", Key = Enum.KeyCode.Z, Hold = 0.10, Delay = 0.25 },
    { Weapon = "Gun", Key = Enum.KeyCode.X, Hold = 0.15, Delay = 0.30 },
}

local function RunFullComboPipeline()
    if AppState.IsExecutingCombo then return end
    AppState.IsExecutingCombo = true

    task.spawn(function()
        while AppState.IsHunting and AppState.SelectedPlayer and not AppState.IsUnderground and not AppState.IsRetreating do
            local targetChar = GetChar(AppState.SelectedPlayer)
            local targetRoot = targetChar and GetRoot(targetChar)
            local targetHum = targetChar and GetHum(targetChar)
            local myChar = GetChar(LocalPlayer)
            local myRoot = myChar and GetRoot(myChar)

            if not targetChar or not targetRoot or not targetHum or targetHum.Health <= 0 or not myRoot then
                break
            end

            local dist = (targetRoot.Position - myRoot.Position).Magnitude
            if dist <= 35 and Config.EnableSkillCombo then
                for _, step in ipairs(OneShotComboChain) do
                    if not AppState.IsHunting or AppState.IsUnderground or AppState.IsRetreating then break end
                    
                    targetChar = GetChar(AppState.SelectedPlayer)
                    targetHum = targetChar and GetHum(targetChar)
                    if not targetChar or not targetHum or targetHum.Health <= 0 then break end

                    -- 1. สลับอาวุธสำหรับสกิลขั้นนี้
                    EquipWeapon(step.Weapon)
                    task.wait(0.04)

                    -- 2. เล็งเป้าหมายตรงกลางตัว
                    pcall(function()
                        Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, targetRoot.Position)
                    end)

                    -- 3. แทรก M1 Weaving
                    if Config.M1Weaving then
                        ExecuteM1Combo()
                    end

                    -- 4. กดปล่อยสกิลตามคีย์
                    pcall(function()
                        VirtualInputManager:SendKeyEvent(true, step.Key, false, game)
                        task.wait(step.Hold)
                        VirtualInputManager:SendKeyEvent(false, step.Key, false, game)
                    end)

                    task.wait(step.Delay)
                end
            else
                task.wait(0.2)
            end
            task.wait(0.1)
        end
        AppState.IsExecutingCombo = false
    end)
end

-- ลูปคอมแบท M1 ต่อเนื่อง
task.spawn(function()
    while true do
        if AppState.IsHunting and AppState.SelectedPlayer and not AppState.IsUnderground and not AppState.IsRetreating then
            local targetChar = GetChar(AppState.SelectedPlayer)
            local myChar = GetChar(LocalPlayer)
            local targetRoot = GetRoot(targetChar)
            local myRoot = GetRoot(myChar)

            if targetChar and targetRoot and myRoot and targetChar.Parent then
                local dist = (targetRoot.Position - myRoot.Position).Magnitude
                if dist <= 32 then
                    ExecuteM1Combo()
                    if not AppState.IsExecutingCombo and Config.EnableSkillCombo then
                        RunFullComboPipeline()
                    end
                end
            end
        end
        task.wait(Config.M1AttackSpeed)
    end
end)

-- =========================================================================
-- PHYSICS & MOVEMENT CONTROLLER
-- =========================================================================
local function ApplyPhysics(myRoot)
    if not myRoot then return end
    local bv = myRoot:FindFirstChild("BountyBV") or Instance.new("BodyVelocity")
    bv.Name = "BountyBV"
    bv.MaxForce = Vector3.new(1e6, 1e6, 1e6)
    bv.Velocity = Vector3.zero
    bv.Parent = myRoot

    local bg = myRoot:FindFirstChild("BountyBG") or Instance.new("BodyGyro")
    bg.Name = "BountyBG"
    bg.MaxTorque = Vector3.new(1e6, 1e6, 1e6)
    bg.P = 18000
    bg.Parent = myRoot
end

local function RemovePhysics(myRoot)
    if not myRoot then return end
    if myRoot:FindFirstChild("BountyBV") then myRoot.BountyBV:Destroy() end
    if myRoot:FindFirstChild("BountyBG") then myRoot.BountyBG:Destroy() end
end

-- ระบบ NoClip ตลอดเวลาเมื่อ ล่า/มุดดิน/เดินทาง
RunService.Stepped:Connect(function()
    if AppState.IsHunting or AppState.IsUnderground or AppState.IsRetreating then
        local myChar = GetChar(LocalPlayer)
        if myChar then
            for _, part in ipairs(myChar:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
    end
end)

local function StopHunt(reason)
    AppState.IsHunting = false
    AppState.IsUnderground = false
    AppState.IsRetreating = false
    AppState.IsExecutingCombo = false
    AppState.CurrentCombatStatus = reason or "Idle"
    local myChar = GetChar(LocalPlayer)
    RemovePhysics(GetRoot(myChar))
end

-- =========================================================================
-- UNDERGROUND BUNKER RETREAT ENGINE (มุดดินหลบภัย 100% ปลอดภัย ไร้บัค)
-- =========================================================================
local function TriggerUndergroundRetreat()
    if AppState.IsUnderground then return end
    AppState.IsHunting = false
    AppState.IsRetreating = false
    AppState.IsExecutingCombo = false
    AppState.IsUnderground = true
    AppState.SelectedPlayer = nil
    AppState.CurrentCombatStatus = "KILL CONFIRMED! Diving Underground (-65 Studs Bunker)..."

    task.spawn(function()
        local myChar = GetChar(LocalPlayer)
        local myRoot = GetRoot(myChar)
        local myHum = GetHum(myChar)
        if not myRoot or not myHum then return end

        ApplyPhysics(myRoot)
        EnsureKen()
        EnsureBuso()

        -- ดำดิ่งลงใต้ดินลึก 65 Studs ทันที
        local safeUndergroundPos = Vector3.new(myRoot.Position.X, myRoot.Position.Y - Config.UndergroundDepth, myRoot.Position.Z)
        
        local startDive = tick()
        while tick() - startDive < 1.0 do
            myChar = GetChar(LocalPlayer)
            myRoot = GetRoot(myChar)
            if not myRoot then break end
            
            local diff = safeUndergroundPos - myRoot.Position
            local bv = myRoot:FindFirstChild("BountyBV")
            if bv then
                bv.Velocity = diff.Unit * 120
            end
            task.wait(0.03)
        end

        local bv = myRoot and myRoot:FindFirstChild("BountyBV")
        if bv then bv.Velocity = Vector3.zero end
        if myRoot then myRoot.CFrame = CFrame.new(safeUndergroundPos) end

        AppState.CurrentCombatStatus = "SAFE BUNKER: Underground Active (-65 Studs) | Regenerating HP..."
    end)
end

-- =========================================================================
-- ANTI-ROLLBACK 3-PHASE SKY GLIDE ENGINE (บินนุ่ม ไร้โรลแบ็ค 100%)
-- =========================================================================
local function SmoothFlyTo(targetPos, destinationName, onComplete)
    task.spawn(function()
        local myChar = GetChar(LocalPlayer)
        local myRoot = GetRoot(myChar)
        local myHum = GetHum(myChar)
        if not myChar or not myRoot or not myHum then return end

        AppState.IsRetreating = true
        AppState.IsHunting = false
        AppState.IsUnderground = false
        AppState.IsExecutingCombo = false
        AppState.CurrentCombatStatus = string.format("Ascending to High Sky (Anti-Rollback)...")

        local travelAltitude = Config.TravelSkyAltitude
        local targetVec = typeof(targetPos) == "CFrame" and targetPos.Position or targetPos

        -- Phase 1: ลอยตัวขึ้นสู่ท้องฟ้าชั้นสูงตรงๆ
        local startLift = tick()
        while tick() - startLift < 2.2 do
            myChar = GetChar(LocalPlayer)
            myRoot = GetRoot(myChar)
            if not myRoot then break end
            
            ApplyPhysics(myRoot)
            EnsureKen()
            EnsureBuso()

            local skyLiftPos = Vector3.new(myRoot.Position.X, travelAltitude, myRoot.Position.Z)
            local diff = skyLiftPos - myRoot.Position
            if diff.Magnitude < 15 then break end

            local bv = myRoot:FindFirstChild("BountyBV")
            if bv then
                bv.Velocity = diff.Unit * Config.TravelFlySpeed
            end
            task.wait(0.03)
        end

        -- Phase 2: บินบนฟ้าชั้นบรรยากาศตรงไปยังปลายทาง
        local skyTargetPos = Vector3.new(targetVec.X, travelAltitude, targetVec.Z)
        while true do
            myChar = GetChar(LocalPlayer)
            myRoot = GetRoot(myChar)
            myHum = GetHum(myChar)
            if not myChar or not myRoot or not myHum or myHum.Health <= 0 then break end

            ApplyPhysics(myRoot)
            EnsureKen()
            EnsureBuso()

            local horizontalDist = (Vector3.new(myRoot.Position.X, 0, myRoot.Position.Z) - Vector3.new(targetVec.X, 0, targetVec.Z)).Magnitude
            AppState.CurrentCombatStatus = string.format("Flying to %s (%dm) [No Rollback]", destinationName or "Destination", math.floor(horizontalDist))

            if horizontalDist <= 20 then
                break
            end

            local dir = (skyTargetPos - myRoot.Position).Unit
            local bv = myRoot:FindFirstChild("BountyBV")
            local bg = myRoot:FindFirstChild("BountyBG")
            if bg then
                bg.CFrame = CFrame.lookAt(myRoot.Position, Vector3.new(targetVec.X, myRoot.Position.Y, targetVec.Z))
            end
            if bv then
                bv.Velocity = dir * Config.TravelFlySpeed
            end
            task.wait(0.03)
        end

        -- Phase 3: ร่อนลงสู่พื้นผิวปลายทางอย่างนุ่มนวล
        local descendStart = tick()
        AppState.CurrentCombatStatus = string.format("Soft Landing at %s...", destinationName or "Destination")
        while tick() - descendStart < 4.0 do
            myChar = GetChar(LocalPlayer)
            myRoot = GetRoot(myChar)
            if not myRoot then break end

            local diff = (targetVec + Vector3.new(0, 3, 0)) - myRoot.Position
            if diff.Magnitude <= 6 then break end

            local bv = myRoot:FindFirstChild("BountyBV")
            if bv then
                bv.Velocity = diff.Unit * math.min(Config.TravelFlySpeed, diff.Magnitude * 14)
            end
            task.wait(0.03)
        end

        myChar = GetChar(LocalPlayer)
        myRoot = GetRoot(myChar)
        local bv = myRoot and myRoot:FindFirstChild("BountyBV")
        if bv then bv.Velocity = Vector3.zero end

        AppState.IsRetreating = false
        AppState.SelectedPlayer = nil
        StopHunt(string.format("Arrived at %s (Safe)", destinationName or "Destination"))
        
        if onComplete then onComplete() end
    end)
end

-- =========================================================================
-- MAIN COMBAT ENGINE LOOP (OVERHEAD HELIX ORBIT)
-- =========================================================================
RunService.Heartbeat:Connect(function()
    if AppState.IsRetreating or AppState.IsUnderground then return end
    if not AppState.IsHunting or not AppState.SelectedPlayer then return end

    local myChar = GetChar(LocalPlayer)
    local myRoot = GetRoot(myChar)
    local myHum = GetHum(myChar)

    if not myChar or not myRoot or not myHum or myHum.Health <= 0 then
        StopHunt("Player Dead")
        return
    end

    local currentHpPercent = (myHum.Health / math.max(1, myHum.MaxHealth)) * 100

    -- Emergency Bunker Dive when low HP
    if Config.EmergencyEscape and (currentHpPercent <= Config.EmergencyHPThreshold) then
        TriggerUndergroundRetreat()
        return
    end

    local targetChar = GetChar(AppState.SelectedPlayer)
    local targetHum = GetHum(targetChar)
    local targetRoot = GetRoot(targetChar)

    -- ตรวจสอบการตายของเป้าหมาย -> มุดดินหลบภัยทันที
    if not targetChar or not targetHum or targetHum.Health <= 0 or not targetChar.Parent then
        TriggerUndergroundRetreat()
        return
    end

    -- ตรวจสอบ SafeZone / PvP
    local st = CheckTargetStatus(AppState.SelectedPlayer)
    if not st.CanAttack then
        StopHunt("Disengaged: " .. st.Reason)
        return
    end

    if not targetRoot then return end

    EnsureBuso()
    EnsureKen()
    TriggerRaceSkill()
    ApplyPhysics(myRoot)

    -- คำนวณตำแหน่งเหนือหัว + หมุนวนหลบสกิล (Helix Orbit)
    local orbitAngle = tick() * Config.OrbitSpeed
    local xOffset = math.cos(orbitAngle) * Config.OrbitRadius
    local zOffset = math.sin(orbitAngle) * Config.OrbitRadius
    
    local verticalJitter = Config.EnableEvasiveJitter and (math.sin(tick() * 8) * 1.2) or 0
    local targetHeight = Config.OverheadHeight + verticalJitter

    local targetOverheadPos = targetRoot.Position + Vector3.new(xOffset, targetHeight, zOffset)
    local currentPos = myRoot.Position
    local dist = (targetOverheadPos - currentPos).Magnitude

    local bg = myRoot:FindFirstChild("BountyBG")
    if bg then
        bg.CFrame = CFrame.lookAt(myRoot.Position, targetRoot.Position)
    end

    if dist > 4.5 then
        AppState.CurrentCombatStatus = "Rushing to Overhead Position (" .. math.floor(dist) .. "m)"
        local dir = (targetOverheadPos - currentPos).Unit
        local bv = myRoot:FindFirstChild("BountyBV")
        if bv then
            bv.Velocity = dir * math.min(Config.FlySpeed, dist * 32)
        end
    else
        local bv = myRoot:FindFirstChild("BountyBV")
        if bv then bv.Velocity = Vector3.zero end
        
        myRoot.CFrame = CFrame.lookAt(targetOverheadPos, targetRoot.Position)
        Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, targetRoot.Position)
        AppState.CurrentCombatStatus = "Executing One-Shot Combo (Melee/Sword/Fruit/Gun)"
    end
end)

-- =========================================================================
-- GUI DASHBOARD (WITH COMBO & SAFETY CONTROLS)
-- =========================================================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BountyGodUniversal"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = (gethui and gethui()) or CoreGui

local ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "FloatingToggle"
ToggleButton.Size = UDim2.new(0, 48, 0, 48)
ToggleButton.Position = IsMobile and UDim2.new(0, 15, 0.4, 0) or UDim2.new(0, 20, 0.2, 0)
ToggleButton.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
ToggleButton.Text = "⚡"
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.TextSize = 22
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Parent = ScreenGui
Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(1, 0)
local ToggleStroke = Instance.new("UIStroke", ToggleButton)
ToggleStroke.Color = Color3.fromRGB(255, 255, 255)
ToggleStroke.Thickness = 2

local draggingToggle, toggleDragStart, toggleStartPos
ToggleButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        draggingToggle = true
        toggleDragStart = input.Position
        toggleStartPos = ToggleButton.Position
    end
end)
UserInputService.InputChanged:Connect(function(input)
    if draggingToggle and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - toggleDragStart
        ToggleButton.Position = UDim2.new(
            toggleStartPos.X.Scale, toggleStartPos.X.Offset + delta.X,
            toggleStartPos.Y.Scale, toggleStartPos.Y.Offset + delta.Y
        )
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        draggingToggle = false
    end
end)

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainWindow"
MainFrame.Size = IsMobile and UDim2.new(0.94, 0, 0.85, 0) or UDim2.new(0, 570, 0, 500)
MainFrame.Position = IsMobile and UDim2.new(0.03, 0, 0.07, 0) or UDim2.new(0.5, -285, 0.5, -250)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 17, 24)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = not IsMobile
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Color = Color3.fromRGB(40, 55, 80)
MainStroke.Thickness = 1.5

ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

UserInputService.InputBegan:Connect(function(input, gpe)
    if not gpe and (input.KeyCode == Enum.KeyCode.RightShift or input.KeyCode == Enum.KeyCode.LeftControl) then
        MainFrame.Visible = not MainFrame.Visible
    end
end)

local Header = Instance.new("TextLabel")
Header.Size = UDim2.new(1, 0, 0, 38)
Header.BackgroundColor3 = Color3.fromRGB(22, 26, 38)
Header.Text = "  ⚡ BOUNTY GOD v8.2 (PVP COMBO PIPELINE)"
Header.TextColor3 = Color3.fromRGB(255, 255, 255)
Header.TextSize = IsMobile and 11 or 13
Header.Font = Enum.Font.GothamBold
Header.TextXAlignment = Enum.TextXAlignment.Left
Header.Parent = MainFrame
Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 10)

-- QUICK ACTION BUTTONS
local ActionToolbar = Instance.new("Frame")
ActionToolbar.Size = UDim2.new(1, -16, 0, 30)
ActionToolbar.Position = UDim2.new(0, 8, 0, 42)
ActionToolbar.BackgroundTransparency = 1
ActionToolbar.Parent = MainFrame

local BunkerBtn = Instance.new("TextButton")
BunkerBtn.Size = UDim2.new(0.24, -3, 1, 0)
BunkerBtn.Position = UDim2.new(0, 0, 0, 0)
BunkerBtn.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
BunkerBtn.Text = "🕳️ DIVE"
BunkerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
BunkerBtn.Font = Enum.Font.GothamBold
BunkerBtn.TextSize = IsMobile and 10 or 11
BunkerBtn.Parent = ActionToolbar
Instance.new("UICorner", BunkerBtn).CornerRadius = UDim.new(0, 6)

local ComboToggleBtn = Instance.new("TextButton")
ComboToggleBtn.Size = UDim2.new(0.24, -3, 1, 0)
ComboToggleBtn.Position = UDim2.new(0.25, 0, 0, 0)
ComboToggleBtn.BackgroundColor3 = Config.EnableSkillCombo and Color3.fromRGB(255, 140, 0) or Color3.fromRGB(100, 100, 100)
ComboToggleBtn.Text = Config.EnableSkillCombo and "⚔️ COMBO ON" or "⚔️ COMBO OFF"
ComboToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ComboToggleBtn.Font = Enum.Font.GothamBold
ComboToggleBtn.TextSize = IsMobile and 10 or 11
ComboToggleBtn.Parent = ActionToolbar
Instance.new("UICorner", ComboToggleBtn).CornerRadius = UDim.new(0, 6)

local TPCafeBtn = Instance.new("TextButton")
TPCafeBtn.Size = UDim2.new(0.24, -3, 1, 0)
TPCafeBtn.Position = UDim2.new(0.50, 0, 0, 0)
TPCafeBtn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
TPCafeBtn.Text = "☕ CAFE"
TPCafeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
TPCafeBtn.Font = Enum.Font.GothamBold
TPCafeBtn.TextSize = IsMobile and 10 or 11
TPCafeBtn.Parent = ActionToolbar
Instance.new("UICorner", TPCafeBtn).CornerRadius = UDim.new(0, 6)

local ESPToggleBtn = Instance.new("TextButton")
ESPToggleBtn.Size = UDim2.new(0.24, -3, 1, 0)
ESPToggleBtn.Position = UDim2.new(0.75, 0, 0, 0)
ESPToggleBtn.BackgroundColor3 = Config.EnableESP and Color3.fromRGB(40, 167, 69) or Color3.fromRGB(100, 100, 100)
ESPToggleBtn.Text = Config.EnableESP and "👁️ ESP ON" or "👁️ ESP OFF"
ESPToggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ESPToggleBtn.Font = Enum.Font.GothamBold
ESPToggleBtn.TextSize = IsMobile and 10 or 11
ESPToggleBtn.Parent = ActionToolbar
Instance.new("UICorner", ESPToggleBtn).CornerRadius = UDim.new(0, 6)

ComboToggleBtn.MouseButton1Click:Connect(function()
    Config.EnableSkillCombo = not Config.EnableSkillCombo
    ComboToggleBtn.Text = Config.EnableSkillCombo and "⚔️ COMBO ON" or "⚔️ COMBO OFF"
    ComboToggleBtn.BackgroundColor3 = Config.EnableSkillCombo and Color3.fromRGB(255, 140, 0) or Color3.fromRGB(100, 100, 100)
end)

ESPToggleBtn.MouseButton1Click:Connect(function()
    Config.EnableESP = not Config.EnableESP
    ESPToggleBtn.Text = Config.EnableESP and "👁️ ESP ON" or "👁️ ESP OFF"
    ESPToggleBtn.BackgroundColor3 = Config.EnableESP and Color3.fromRGB(40, 167, 69) or Color3.fromRGB(100, 100, 100)
end)

BunkerBtn.MouseButton1Click:Connect(function()
    if AppState.IsUnderground then
        StopHunt("Exited Bunker (Idle)")
    else
        TriggerUndergroundRetreat()
    end
end)

TPCafeBtn.MouseButton1Click:Connect(function()
    SmoothFlyTo(GetCafePos(), "Cafe / Hub")
end)

local StatsCard = Instance.new("Frame")
StatsCard.Size = UDim2.new(1, -16, 0, 56)
StatsCard.Position = UDim2.new(0, 8, 0, 76)
StatsCard.BackgroundColor3 = Color3.fromRGB(24, 29, 42)
StatsCard.Parent = MainFrame
Instance.new("UICorner", StatsCard).CornerRadius = UDim.new(0, 8)

local StatText = Instance.new("TextLabel")
StatText.Size = UDim2.new(1, -16, 1, 0)
StatText.Position = UDim2.new(0, 8, 0, 0)
StatText.BackgroundTransparency = 1
StatText.RichText = true
StatText.Font = Enum.Font.GothamMedium
StatText.TextSize = IsMobile and 11 or 12
StatText.TextColor3 = Color3.fromRGB(230, 235, 245)
StatText.TextXAlignment = Enum.TextXAlignment.Left
StatText.Parent = StatsCard

local function UpdateStatsDisplay()
    local net = Stats.BountyGained - Stats.BountyLost
    local netColor = net >= 0 and "#00FF7F" or "#FF4D4D"
    StatText.Text = string.format(
        "<b>Bounty:</b> %s | <b>K/D:</b> <font color='#00FF7F'>%d</font>/<font color='#FF4D4D'>%d</font><br/>" ..
        "<b>Gained:</b> <font color='#00FF7F'>+%s</font> | <b>Lost:</b> <font color='#FF4D4D'>-%s</font> | <b>Net:</b> <font color='%s'>%+d</font>",
        tostring(Stats.CurrentBounty), Stats.Kills, Stats.Deaths,
        tostring(Stats.BountyGained), tostring(Stats.BountyLost),
        netColor, net
    )
end

local function SetupBountyListener()
    local ls = LocalPlayer:WaitForChild("leaderstats", 10)
    if not ls then return end
    local bStat = ls:FindFirstChild("Bounty/Honor") or ls:FindFirstChild("Bounty") or ls:FindFirstChild("Honor")
    if bStat then
        bStat.Changed:Connect(function(newVal)
            local diff = newVal - Stats.CurrentBounty
            if diff > 0 then
                Stats.BountyGained = Stats.BountyGained + diff
                Stats.Kills = Stats.Kills + 1
                -- ฆ่าได้สำเร็จ -> มุดดินหลบภัยทันที
                TriggerUndergroundRetreat()
            elseif diff < 0 then
                Stats.BountyLost = Stats.BountyLost + math.abs(diff)
                Stats.Deaths = Stats.Deaths + 1
            end
            Stats.CurrentBounty = newVal
            UpdateStatsDisplay()
        end)
    end
end
SetupBountyListener()
UpdateStatsDisplay()

local ScrollList = Instance.new("ScrollingFrame")
ScrollList.Size = UDim2.new(1, -16, 1, -200)
ScrollList.Position = UDim2.new(0, 8, 0, 136)
ScrollList.BackgroundColor3 = Color3.fromRGB(11, 13, 18)
ScrollList.ScrollBarThickness = IsMobile and 6 or 4
ScrollList.Parent = MainFrame
Instance.new("UICorner", ScrollList).CornerRadius = UDim.new(0, 8)

local ListLayout = Instance.new("UIListLayout", ScrollList)
ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
ListLayout.Padding = UDim.new(0, 4)

local ActionCard = Instance.new("Frame")
ActionCard.Size = UDim2.new(1, -16, 0, 55)
ActionCard.Position = UDim2.new(0, 8, 1, -60)
ActionCard.BackgroundTransparency = 1
ActionCard.Parent = MainFrame

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Size = UDim2.new(0.55, 0, 1, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "Target: [None] | Status: Idle"
StatusLabel.TextColor3 = Color3.fromRGB(170, 185, 210)
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.TextSize = IsMobile and 10 or 11
StatusLabel.TextXAlignment = Enum.TextXAlignment.Left
StatusLabel.Parent = ActionCard

local HuntBtn = Instance.new("TextButton")
HuntBtn.Size = UDim2.new(0.42, 0, 0.85, 0)
HuntBtn.Position = UDim2.new(0.58, 0, 0.07, 0)
HuntBtn.BackgroundColor3 = Color3.fromRGB(40, 167, 69)
HuntBtn.Text = "START HUNT (ENGAGE)"
HuntBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
HuntBtn.Font = Enum.Font.GothamBold
HuntBtn.TextSize = IsMobile and 10 or 11
HuntBtn.Parent = ActionCard
Instance.new("UICorner", HuntBtn).CornerRadius = UDim.new(0, 6)

local function RenderRadarRows()
    for _, child in ipairs(ScrollList:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            local st = CheckTargetStatus(player)
            local hpPercent = math.floor((st.Health / math.max(1, st.MaxHealth)) * 100)

            local Row = Instance.new("Frame")
            Row.Size = UDim2.new(1, -8, 0, 48)
            Row.BackgroundColor3 = (AppState.SelectedPlayer == player) and Color3.fromRGB(45, 65, 105) or Color3.fromRGB(19, 23, 33)
            Row.Parent = ScrollList
            Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

            local statusTag = ""
            if st.CanAttack then
                statusTag = "<font color='#00FF7F'>[READY]</font>"
            elseif st.InSafeZone then
                statusTag = "<font color='#FFD700'>[SAFE ZONE]</font>"
            elseif not st.PvPEnabled then
                statusTag = "<font color='#FF5555'>[PVP OFF]</font>"
            elseif not st.IsAlive then
                statusTag = "<font color='#888888'>[DEAD]</font>"
            else
                statusTag = "<font color='#FF8800'>[" .. st.Reason:upper() .. "]</font>"
            end

            local Info = Instance.new("TextLabel")
            Info.Size = UDim2.new(0.60, 0, 1, 0)
            Info.Position = UDim2.new(0, 8, 0, 0)
            Info.BackgroundTransparency = 1
            Info.RichText = true
            Info.Font = Enum.Font.Gotham
            Info.TextSize = IsMobile and 10 or 11
            Info.TextColor3 = Color3.fromRGB(240, 240, 240)
            Info.TextXAlignment = Enum.TextXAlignment.Left
            
            Info.Text = string.format(
                "<b>%s</b> %s | <font color='#00E5FF'>Lv.%d</font><br/>" ..
                "<font color='#00FF7F'>HP: %d%%</font> (%d/%d) | <font color='#FFD700'>%dm</font> | B: %s",
                player.DisplayName, statusTag, st.Level,
                hpPercent, st.Health, st.MaxHealth, st.Distance, tostring(st.Bounty)
            )
            Info.Parent = Row

            local SpyBtn = Instance.new("TextButton")
            SpyBtn.Size = UDim2.new(0, IsMobile and 46 or 50, 0, 26)
            SpyBtn.Position = UDim2.new(0.62, 0, 0.5, -13)
            SpyBtn.BackgroundColor3 = Color3.fromRGB(48, 85, 150)
            SpyBtn.Text = (AppState.IsSpectating and AppState.SelectedPlayer == player) and "UNSPY" or "SPY"
            SpyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            SpyBtn.Font = Enum.Font.GothamBold
            SpyBtn.TextSize = 10
            SpyBtn.Parent = Row
            Instance.new("UICorner", SpyBtn).CornerRadius = UDim.new(0, 4)

            SpyBtn.MouseButton1Click:Connect(function()
                if AppState.IsSpectating and AppState.SelectedPlayer == player then
                    Camera.CameraSubject = GetHum(GetChar(LocalPlayer))
                    AppState.IsSpectating = false
                    SpyBtn.Text = "SPY"
                else
                    local targetHum = GetHum(GetChar(player))
                    if targetHum then
                        Camera.CameraSubject = targetHum
                        AppState.IsSpectating = true
                        AppState.SelectedPlayer = player
                        SpyBtn.Text = "UNSPY"
                    end
                end
            end)

            local SelBtn = Instance.new("TextButton")
            SelBtn.Size = UDim2.new(0, IsMobile and 54 or 62, 0, 26)
            SelBtn.Position = UDim2.new(IsMobile and 0.77 or 0.78, 0, 0.5, -13)
            
            if not st.CanAttack then
                SelBtn.BackgroundColor3 = Color3.fromRGB(40, 44, 52)
                SelBtn.Text = st.InSafeZone and "SAFE" or "NO PVP"
                SelBtn.TextColor3 = Color3.fromRGB(120, 125, 135)
            else
                SelBtn.BackgroundColor3 = (AppState.SelectedPlayer == player) and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(0, 140, 80)
                SelBtn.Text = (AppState.SelectedPlayer == player) and "LOCKED" or "SELECT"
                SelBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
            
            SelBtn.Font = Enum.Font.GothamBold
            SelBtn.TextSize = 10
            SelBtn.Parent = Row
            Instance.new("UICorner", SelBtn).CornerRadius = UDim.new(0, 4)

            SelBtn.MouseButton1Click:Connect(function()
                if not st.CanAttack then
                    StatusLabel.Text = "Blocked: " .. st.Reason
                    return
                end

                AppState.SelectedPlayer = player
                StatusLabel.Text = string.format("Target: %s (Lv.%d | Ready)", player.DisplayName, st.Level)
                RenderRadarRows()
            end)
        end
    end
    ScrollList.CanvasSize = UDim2.new(0, 0, 0, ListLayout.AbsoluteContentSize.Y + 10)
end

task.spawn(function()
    while true do
        if not AppState.IsHunting and not AppState.IsRetreating and not AppState.IsUnderground then
            RenderRadarRows()
        else
            StatusLabel.Text = "Status: " .. AppState.CurrentCombatStatus
        end
        task.wait(1.5)
    end
end)

HuntBtn.MouseButton1Click:Connect(function()
    if AppState.IsHunting or AppState.IsRetreating or AppState.IsUnderground then
        StopHunt("Hunt Cancelled")
        HuntBtn.Text = "START HUNT (ENGAGE)"
        HuntBtn.BackgroundColor3 = Color3.fromRGB(40, 167, 69)
        StatusLabel.Text = "Status: Hunt Cancelled (Idle)"
    else
        if not AppState.SelectedPlayer then
            StatusLabel.Text = "Error: Select target first!"
            return
        end

        local st = CheckTargetStatus(AppState.SelectedPlayer)
        if not st.CanAttack then
            StatusLabel.Text = "Cannot Hunt: " .. st.Reason
            AppState.SelectedPlayer = nil
            RenderRadarRows()
            return
        end

        EquipWeapon(Config.PrimaryWeapon)

        AppState.IsUnderground = false
        AppState.IsHunting = true
        HuntBtn.Text = "STOP HUNTING"
        HuntBtn.BackgroundColor3 = Color3.fromRGB(220, 53, 69)
        StatusLabel.Text = "Status: Engaging Target (One-Shot Combo Pipeline)..."
    end
end)
